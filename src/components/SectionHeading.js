import React from 'react'

const SectionHeading = ({title}) => {
    return (
        <h1 className="font-montserrat font-bold text-[#2E2E2E] text-2xl">
            {title}
        </h1>
    )
}

export default SectionHeading
